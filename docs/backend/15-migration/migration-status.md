# Migration Status

Per `docs/controls/BACKEND_KNOWLEDGE_POLICY.md` §8. Status scale (do not skip stages):

```
FRAPPE_REFERENCE → DOCUMENTED → CONTRACT_DEFINED → NATIVE_DEVELOPMENT → SHADOW_TESTING → NATIVE_PRODUCTION → FRAPPE_RETIRED
```

**No domain has begun native development.** A `DOCUMENTED` status below means backend knowledge
capture has started for that domain in `docs/backend/`, not that a contract or native code exists.

| Domain | Status | Notes |
|---|---|---|
| Master Data (Item, Item Group, UOM, Warehouse, Customer, Supplier, Contact, Address, Territory, Price List) | `DOCUMENTED` | MD-R2 baseline (2026-09-22): live-schema-verified (`get_doctype_fields`) and live-sample-verified (`list_documents`) documentation for all 10 doctypes in `docs/backend/01-master-data/` (`item.md`, `customer-supplier.md`, `secondary-masters.md`, `warehouse.md`, `price-list.md`). BOM is cross-referenced from the same folder (`bom.md`) to its existing canonical doc at `docs/backend/05-manufacturing/bom.md`, not duplicated. Frontend: all 4 shipped Master Data domain packages are now independently ACCEPTED — Item, Business Partner, Inventory Structure/Warehouse, and, as of 2026-09-22, BOM. BOM's history: Package 4A was never independently reviewed before `MD-R1`; Package 4B was independently reviewed by Codex and returned `CHANGES REQUIRED`, partially remediated by `6c38f7b`; `MD-R1` (2026-09-22) then independently reviewed the complete shipped BOM surface, also `CHANGES REQUIRED`; `23886ac` remediated those findings; a separate-account/session governance confirmation subsequently accepted that remediation — see `docs/master-data-architecture.md` §9 for the full chronology. Five `MD-UNV-*` items open (see `99-unverified/unverified-behaviours.md`), most significantly `MD-UNV-003`: the real ERPNext Customer/Supplier↔Contact/Address relationship (`Dynamic Link`) is not wired up anywhere in this frontend. Supplier Group, Operation, Workstation, Company, Cost Center, Project, UOM (as its own screen) remain `FRAPPE_REFERENCE` — no frontend built, not documented as their own entities yet. |
| Sales (Quotation → Sales Order → Delivery Note → Sales Invoice, Pick List) | `DOCUMENTED` | Frontend core hardened per Current Mission; `docs/backend/02-sales/` docs written for Quotation, Sales Order, Delivery Note, Sales Invoice, Pick List, Secondary Sales Masters (Sales Partner, Sales Person, Campaign), and Selling Settings. Sales Return and Credit Note specs (`sales-return.md`, `credit-note.md`) exist as `FRAPPE_REFERENCE` ready for frontend implementation. |
| Purchasing / Buying (PO → Purchase Receipt → Purchase Invoice) | `DOCUMENTED` | Core cycle accepted 2026-09-16; `docs/backend/03-purchasing/` docs written for PO, Purchase Receipt, and Purchase Invoice. |
| Inventory / Stock (Warehouses, Batches, Serial Nos, Stock Entry, Stock Balance) | `DOCUMENTED` | MVP shipped 2026-09-16; `docs/backend/04-inventory/` docs written for Stock Entry, Batch, and Serial No. Directly touched by Manufacturing's Material Transfer (Stock Entry), documented from the Manufacturing side only — see `docs/backend/05-manufacturing/material-transfer.md`. |
| **Manufacturing — Work Order + Material Transfer for Manufacture + Manufacture completion + Cancel** | `DOCUMENTED` | Baseline pass 2026-09-17 (Work Order list/detail/create, Material Transfer for Manufacture). Extended 2026-09-22/23 (`MFG-CLOSE-1`): the Manufacture Stock Entry / "Complete Production" flow — see `docs/backend/05-manufacturing/manufacture-completion.md`. Source-verified via live SSH read of the installed ERPNext v16.34.2, then `Runtime Test: VERIFIED` via a disposable-fixture live QA pass (full/partial production, over-production rejection, Draft-WO attempt — all against throwaway data, cleaned up, independently re-confirmed absent; the 4 real Work Orders untouched). Accounting impact also `Runtime Test: VERIFIED` (see that doc's "Accounting impact" section — includes a real finding that this instance has no distinct per-warehouse stock GL accounts configured). Extended again 2026-09-23 (`MFG-WO-LC-1`): Work Order Cancel — see `work-order.md`'s "Cancel contract" section. `Runtime Test: VERIFIED` (independently re-run by `qa-tester`, 2026-09-23, against fresh `TEST-WOLC-QA-*` fixtures after an earlier draft's claimed verification was found unsubstantiated — see `QA_LOG.md`'s `MFG-WO-LC-1` entry) covering the full dependency matrix (Stock Entry, Job Card, Production Plan cascade); Material Request, Stock Reservation Entry, and Pick List/Serial No back-links remain `NEEDS_VERIFICATION` (non-blocking). |
| Manufacturing — BOM (as an entity) | `FRAPPE_REFERENCE` | Read-only canonical entity frontend shipped 2026-09-19 (Package 4A): `/master-data/boms` (list) + `/master-data/boms/[name]` (detail) — header/status, Components, Operations, Costing, More Info; `bom_no` plain-text references converted to entity links. Create + Draft-only edit shipped same day (Package 4B): `/master-data/boms/new` and an explicit "Edit BOM" action on the detail page (`?edit=1`) when `docstatus === 0`, via `createDoc`/`updateDoc` — see `docs/backend/05-manufacturing/bom.md`'s "Mutation contract" section for the exact field set. A same-day remediation (`CX-MFG-BOM-4B-001`/`002`) added a narrow submitted-BOM Activate/Deactivate + Set as Default action (`is_active`/`is_default` only, source-verified `allow_on_submit` on the real `BOM` DocType) and made the Draft edit form opt-in instead of automatic — see the same document's "Submitted-BOM availability contract" section. Submit (`MFG-CLOSE-0c`, 2026-09-22) and Cancel/Amend (`MFG-CLOSE-2`, 2026-09-23) have since shipped and were live-verified against disposable fixtures — see `bom.md`'s "Submit contract" and "Cancel/Amend contract" sections. Only "Update Cost" remains unbuilt. The non-Draft-update rejection and zero-rate-component acceptance from the original Package 4B write-up remain `NEEDS_VERIFICATION` (not exercised by the later Submit/Cancel/Amend live testing). Full schema/lifecycle/costing/multi-level/Operation-Routing-Workstation-classification investigation remains in `docs/backend/05-manufacturing/bom.md`; lifecycle/multi-level/costing-recompute behavior itself is still `NEEDS_VERIFICATION` (`MFG-UNV-009`) — this frontend does not resolve that. |
| Manufacturing — Job Card, Workstations, OEE | `DOCUMENTED` (Job Card model/lifecycle + read-only frontend + Cancel; Workstations/OEE still `FRAPPE_REFERENCE`) | Job Card discovery/architecture pass (`MFG-JOBCARD-0`, 2026-09-23): full model, lifecycle (including a live-confirmed `status`-field-goes-stale-after-cancel quirk), time-log, quantity, and Cancel contract `SOURCE VERIFIED`/`LIVE VERIFIED` via one disposable fixture (`TEST-JOBCARD-0-*`, cleaned up) — see `docs/backend/05-manufacturing/job-card.md`. Read-only list/detail frontend (`MFG-JOBCARD-1`, 2026-09-23) shipped: `/manufacturing/job-cards` + `/manufacturing/job-cards/[name]`, `docstatus`-first lifecycle display, real Work Order ↔ Job Card cross-links — independently live-QA'd (zero-mutation field-shape verification against the real instance) and code-reviewed, no blocking findings. Cancel (`MFG-JOBCARD-LC-1`, 2026-09-23) shipped on top: native `cancelDoc("Job Card", name)`, no proactive dependency check (the real blocker, `validate_produced_quantity()`'s `JobCardCancelError`, isn't a simple back-link). `Runtime Test: VERIFIED` (independent `qa-tester` pass, fresh `TEST-JOBCARDLC-*` fixtures) covering Draft/Submitted/Cancelled/nonexistent cancel attempts, the Job-Card-blocks-Work-Order-cancel unblock cycle, and — resolving the previously-open `MFG-UNV-014` — the Manufacture-Stock-Entry-blocks-Job-Card-cancel case, with exact error text and the live-confirmed reversal order (cancel Manufacture Stock Entry → cancel Job Card → cancel Work Order). Execution/time-log actions (`MFG-JOBCARD-2`) remain a future package. Workstations and OEE remain undiscovered, each their own future scoped package per the Current Mission priority lock. |
| Manufacturing — Production Plan | `FRAPPE_REFERENCE` | Zero frontend footprint. Discovery/canonicalization pass 2026-09-19 (schema + `frappe/erpnext` source, no live Production Plan document exists on this instance to test against): header/child-table schema, Sales Order sourcing eligibility, `combine_items`/`combine_sub_items` behavior, multi-BOM resolution per row, sub-assembly explosion and `type_of_manufacturing` branching, material requirement formula, Work Order/Material Request generation and back-references — all documented in `docs/backend/05-manufacturing/production-plan.md`, flagged `MFG-UNV-012` pending live verification (renumbered 2026-09-19 from a colliding `MFG-UNV-010` per `CX-MFG-PP-004`). Recommended next package: PP-1 (canonical read-only List/Detail). |
| Accounting / Finance (Chart of Accounts, Bank Account, GL, AR, AP) | `DOCUMENTED` (Chart of Accounts full maintenance + Bank Account full CRUD; GL/AR/AP/Payment Entry/Journal Entry still `FRAPPE_REFERENCE`) | FIN-0 discovery/architecture pass (2026-09-24): full live ERPNext v16.34.2 Finance doctype inventory, ownership resolution (`FIN-GOV-1`, same day — Finance owns Chart of Accounts/Account, Journal Entry, Payment Entry, Bank Account/Transaction, Cost Center), and the FIN-1..FIN-6 build sequence — see `docs/backend/06-accounting/finance-architecture.md`. **FIN-1** (same day) shipped and live-verified the first two: read-only Chart of Accounts tree (`/accounting/chart-of-accounts`, 96 accounts/company, zero orphan parent references confirmed) and full Bank Account CRUD (`/accounting/bank-accounts`, including a live-tested get-or-create resolution for the mandatory `Bank` link, which had zero records on this tenant) — see `docs/backend/06-accounting/chart-of-accounts-bank-account.md`. **FIN-1E** (same day) extended Chart of Accounts from read-only to full maintenance (Create/Edit/Enable-Disable/Delete-where-safe, root-account protection, contextual child-account creation) — live-verified via a full disposable-fixture create→edit→invalid-hierarchy→disable→rename→delete lifecycle run through `bench console`, tenant account counts confirmed unchanged (96/96 per company) after cleanup. Payment Entry + AR/AP visibility (FIN-2), Journal Entry (FIN-3), and native General Ledger/Trial Balance/P&L/Balance Sheet reports (FIN-4) remain unbuilt; GL impact of Sales/Buying/Manufacturing transactions is largely still `NEEDS_VERIFICATION` — see `docs/backend/99-unverified/unverified-behaviours.md`. |
| Tax | `FRAPPE_REFERENCE` | Not yet documented. |
| Workflows / permissions / document lifecycle (cross-cutting) | `FRAPPE_REFERENCE` | Draft/Submit/Cancel/update-after-submit behavior documented per-domain as encountered (see Work Order's lifecycle section), not yet as its own cross-cutting doc. |

## Migration order (policy §8, do not begin out of order without justification)

Lower to higher complexity: Master Data → CRM → Sales → Purchasing → Workflow → Inventory →
Manufacturing → Tax → Accounting/GL. Accounting, inventory valuation, manufacturing costing, and
financial statements stay on Frappe the longest.

## Next candidates for `DOCUMENTED` status

Ordered by what's already built and reviewed in the frontend (per `PROGRESS.md`) but still
undocumented here: **Sales** (largest surface, hardened first per Current Mission) → **Inventory
MVP** → **Buying core cycle**. Manufacturing's remaining pieces (Job Card, BOM, Workstations, OEE)
follow only as their own frontend packages get built, per the Current Mission priority lock in
`CLAUDE.md`.
