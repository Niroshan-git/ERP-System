# infra/scripts

Operational scripts: server setup, MariaDB/Postgres backup scripts,
deployment helpers, health checks. Nothing here should contain real
passwords, API keys, or the SSH private key — reference environment
variables or a password manager instead (per CLAUDE.md ground rules).
